//! Bench harness. Lane owns: `mock` subcommand (deterministic SSE upstream)
//! and `run` subcommand (concurrent TTFT sampler producing JSON+MD report).
fn main() {
    // Lane implements.
    eprintln!("bench: not built yet (lane pending)");
    std::process::exit(78);
}
